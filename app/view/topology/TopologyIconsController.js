Ext.define('Esprit.view.topology.TopologyIconsController', {
	extend: 'Ext.app.ViewController',
		
	alias: 'controller.topologyicons',
	
	beforeRender: function(){
		/*
	    var me = this;
		
		var store = Ext.create('Esprit.store.TopologyIcons');
		
		store.load({scope: me, callback: function(records, operation, success) {
				if(success){
					var arr = new Array();
					for(r in records){
						arr.push(records[r].data);
					}					
					me.lookupReference('createtopoloty').setData(arr);
				}
			}
		});
		*/
	}		
});